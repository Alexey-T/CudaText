name=class declaration (with Create/Destroy)
id=classc
lex=Pascal
text=
type
  ${1:TName} = class(${2:TObject})
  private
    ${3://declaration}
  protected

  public
	constructor Create; override;
	destructor Destroy; override;
  published

  end;